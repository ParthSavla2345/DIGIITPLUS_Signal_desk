# SignalDesk — System Architecture

## Overview

SignalDesk is a full-stack AI-powered incident intelligence platform. It uses Gemini AI for triage and RAG-grounded investigation, Supabase pgvector for semantic retrieval, and an evidence-scoring system to prevent AI hallucinations.

---

## System Architecture Diagram

```mermaid
graph TB
    User["👤 User / Engineer"]

    subgraph Frontend["React + Vite + TypeScript + Tailwind CSS\n(port 5173)"]
        Dashboard["📊 Dashboard"]
        IncidentList["🎫 Incidents List"]
        CreatePage["➕ Create Incident"]
        DetailPage["🔍 Incident Detail"]
        KBPage["📚 Knowledge Base"]
    end

    subgraph Backend["Node.js + Express + TypeScript\n(port 5000)"]
        API["REST API Layer\n/api/incidents\n/api/knowledge"]
        Controller["Incident Controller\nBusiness Logic"]
        TriageService["🤖 AI Triage Service\ngemini-2.0-flash"]
        EmbeddingService["🧠 Embedding Service\ngemini-embedding-001\n768 dimensions"]
        RagPipeline["🔎 RAG Pipeline\nVector Retrieval + Metadata Boost"]
        AnalysisService["🤖 AI Analysis Service\nEvidence-grounded Investigation"]
        AnomalyDetector["🚨 Anomaly Detector\nNovel + Cluster Detection"]
        EvidenceScorer["🛡️ Evidence Scorer\nBackend-computed Score"]
    end

    subgraph Gemini["Google Gemini API"]
        GeminiGen["gemini-2.0-flash\nTriage + Investigation"]
        GeminiEmb["gemini-embedding-001\n768-dim Embeddings"]
    end

    subgraph Supabase["Supabase PostgreSQL + pgvector"]
        IncidentsTable["incidents\n(with embedding vector(768))"]
        KnowledgeTable["resolved_incident_knowledge\n(with embedding vector(768))"]
        ArticlesTable["knowledge_articles\n(with embedding vector(768))"]
        ActivityTable["incident_activity\n(timeline)"]
        VectorSearch["match_resolved_incidents()\nmatch_knowledge_articles()\ndetect_incident_cluster()"]
    end

    subgraph Seed["Seed Pipeline"]
        HFDataset["Hugging Face Dataset\nmindweave/help-desk-tickets\n(Online API, no local download)"]
        SeedScript["seedDataset.ts\nDynamic Column Detection"]
        KBScript["seedKnowledgeBase.ts\n7 IT Articles"]
    end

    User --> Frontend
    Frontend --> API
    API --> Controller
    Controller --> TriageService
    Controller --> EmbeddingService
    Controller --> RagPipeline
    Controller --> AnalysisService
    Controller --> AnomalyDetector

    TriageService --> GeminiGen
    EmbeddingService --> GeminiEmb
    AnalysisService --> GeminiGen
    RagPipeline --> VectorSearch
    RagPipeline --> EvidenceScorer
    AnomalyDetector --> VectorSearch

    Controller --> IncidentsTable
    Controller --> ActivityTable
    VectorSearch --> KnowledgeTable
    VectorSearch --> ArticlesTable

    HFDataset --> SeedScript
    SeedScript --> GeminiEmb
    SeedScript --> KnowledgeTable
    KBScript --> GeminiEmb
    KBScript --> ArticlesTable
```

---

## Key Design Decisions

### 1. Persistence-First Flow
Incidents are saved to Supabase **before** any AI processing. If Gemini fails, the ticket is never lost. AI runs in the background after the HTTP response returns.

### 2. Backend-Only AI
All Gemini API calls happen server-side. The `GEMINI_API_KEY` and `SUPABASE_SECRET_KEY` are never exposed to the browser.

### 3. Backend-Computed Evidence Score
The evidence strength score (0–100) is computed from retrieval quality metrics in TypeScript code — not generated by Gemini. This prevents AI from inflating its own confidence.

### 4. Hallucination Guard
When evidence strength is below threshold, the system explicitly tells users evidence is insufficient rather than generating an unsupported recommendation.

### 5. Explainable Anomaly Detection
Both anomaly types (novel incident, incident cluster) are detected by backend logic — similarity thresholds and time-window database queries — not by Gemini judgment.
